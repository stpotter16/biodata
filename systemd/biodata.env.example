# Copy to /etc/biodata/biodata.env, chown root:biodata, chmod 640

PORT=8080
BIODATA_DB_PATH=/var/lib/biodata

# Secrets - required, generate with `make secrets/hmac` or similar
BIODATA_PASSPHRASE=
BIODATA_SESSION_ENV_KEY=
